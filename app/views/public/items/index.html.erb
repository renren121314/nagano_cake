<h2>商品一覧</h2><p>（全<%= @items.count %>件）</p>

<% @items.each do |item| %>
<div>
  <p>
    <%= link_to public_item_path(item.id) do %>
      <%= image_tag item.get_image %>
    <% end %>
  </p>
  <p><%= item.name %></p>
  <p>¥<%= item.price %></p>
</div>
<% end %>